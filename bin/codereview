#! /usr/bin/env python

import sys

from codereview import run_command

if len(sys.argv) < 2:
    print 'Usage: %s <command> [command arguments...]' % sys.argv[0]
    sys.exit(1)

command = sys.argv[1]
arguments = sys.argv[2:]

run_command(command, arguments)
